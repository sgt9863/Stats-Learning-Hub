# DESIGN_LOG — デザイン改善ログ

デザイン方針は [DESIGN_SYSTEM.md](DESIGN_SYSTEM.md)。1イテレーション=1テーマで記録する。
書式: 日時 / 改善テーマ / 変更内容 / 影響範囲 / before-afterで良くなった点 / 次の改善候補。

---

## 2026-07-05 — Iter.D1: カラーシステムの確立とグラフとの色分離

**改善テーマ**: デザインシステムをトークンとして確定し、UIアクセント色をグラフ系列色から分離する。

**変更内容**
- `DESIGN_SYSTEM.md` を新設。カラー／タイポ／余白／コンポーネントのトークンを一元定義。ダークモードは当面非対応と決定（理由も記載）。
- **UIアクセントを `#4f46e5`（インディゴ）→ `#3b4557`（低彩度スレート墨）に変更**。旧色はグラフ系列1 `#4f6df5`（青）とほぼ同一で、UIクロムとグラフが同化していた。
- 散在していたハードコード色を CSS 変数化: `--surface` `--muted-2` `--nav-ink` `--hover` `--warn-bg/line/bar` を新設し、サイドバー文字色・ホバー面・nav-group・例題ボックスをトークン参照に統一。
- 注記(`.note`)・まとめ(`.keypoints`)・クイズ解説(`.quiz-explain`)の背景を旧インディゴ面 `#eef0ff` ＋枠 `#dfe2fb` から無彩色 `--surface`＋`--line` に変更。
- `::selection` のインディゴを同スレートに合わせて変更。
- CSS変数のみの変更で、HTML構造・JS・グラフ描画コード（`app.js` の系列色）は一切触れていない。

**影響範囲**: 全ページの UI クロム（サイドバー選択・見出し `h3` の縦バー・カード左バー・スライダーつまみ・値バッジ・クイズ番号・ロードマップ badge・注記/まとめ/解説ボックス）。グラフ内部の描画色・数式・本文コンテンツは不変。

**before-afterで良くなった点**（1300px / 420px 実機スクショで確認）
- デモの青い棒（グラフ系列1）が**ページ唯一の青**として際立つようになった。従来は同色のインディゴがサイドバー・注記・見出しバーに散っており、グラフの色が「意味を持つ信号」として弱かった。
- ページ全体が低彩度スレート＋墨の無彩色基調になり、「落ち着いた学術的ミニマル」の方向に前進。装飾的な彩度を足さずに、グラフだけが色を持つ構図を実現。
- 色がトークン化され、以降の改善で参照一元管理が可能に。
- 数式（KaTeX）レンダリング・グラフの操作性・コントラストは不変。クイズ正誤色（teal/朱）も温存。ロードマップの+αトラックは朱(`--accent2`)を残し「範囲外」の区別を維持。

**次の改善候補**
1. **本文の読み幅（measure）制御**: `#main` の本文ブロックが広い画面で1行が長くなりがち。全角約40字を目安に読み幅上限を設ける（DESIGN_SYSTEM のレイアウト方針に沿う）。
2. **タイポグラフィ階層の微調整**: 見出しと本文の余白・行間の再点検（h3 直後の詰まり、リスト内の行間など）。
3. **注記/まとめ/例題ボックスの視覚的統一**: アイコン・左バー・余白のルールを揃え、種類の区別を彩度でなく形で表す。

---

## 2026-07-05 — Iter.D2: 本文の読み幅（measure）制御

**改善テーマ**: 本文プロースの1行の字数を抑えて可読性を上げる（図表・デモは全幅のまま）。

**変更内容**
- `--measure: 42em`（全角約42字）を新設。`.lead` と `.body` 直下のプロース（`p` / `ul` / `ol` / `h3` / `.note`）に `max-width: var(--measure)` を適用。
- **図表・デモ・数式ブロックは読み幅に縛らない**：`table.simple`（`.body` 直下でも `display:block` で全幅スクロール）、`section.demo` / `.example` / `.keypoints` / `.quiz`（`.body` の兄弟要素）はいずれも対象外＝全幅を維持。
- CSS変数＋子セレクタのみの変更。HTML構造・JS・グラフ描画には未介入。

**影響範囲**: トピックページの本文プロース幅のみ。デスクトップ幅で効き、モバイル（幅 < 42em）では従来どおり全幅。

**before-afterで良くなった点**（1360px / 420px 実機スクショで確認）
- 本文が端から端まで伸びず、約42字で折り返す読みやすい1カラムになった。視線の戻り幅が短くなり、情報密度の高い解説でも追いやすい。
- 一方で**前提条件の表・デモのグラフ・まとめ・クイズは全幅を維持**し、グラフが主役の構図は不変。テキスト列＜図表という自然な編集レイアウトに。
- 数式（KaTeX display）は読み幅の中で中央寄せ、崩れなし。モバイルは全幅のまま回帰なし。

**次の改善候補**
1. **注記/まとめ/例題ボックスの視覚的統一**: アイコン位置・左バー・余白のルールを揃え、種類の区別を彩度でなく形・ラベルで表す。
2. **タイポグラフィ階層の微調整**: h3 直後の余白、リスト行間、`.lead` のサイズ感の再点検。
3. **サイドバーの視線誘導**: グループ見出しと項目のインデント・現在地表示の強調度を調整。

---

## 2026-07-05 — Iter.D3: コールアウト系ボックスの視覚統一

**改善テーマ**: 注記・まとめ・例題を「角丸ボックス＋4px左アクセントバー」の共通言語に揃え、一貫性を出す（種類は形＋1色で区別、彩度は増やさない）。

**変更内容**
- 共通パターンを確定：全コールアウトを `border-radius: var(--radius)` の角丸＋4px左バーに統一。
- `.note`：左フラット角丸（`0 8px 8px 0`）→ 全角丸 `var(--radius)`。左バーは `--accent`（スレート）を維持。
- `.keypoints`：左バー無し → `border-left: 4px solid var(--accent)`（スレート）を追加。`.note` と同じ「情報系」の見た目に。
- `.example`：`border-left: 4px solid var(--warn-bar)`（琥珀）を追加。「実践系（worked problem）」を暖色バーで区別（※現状 example ブロックを使うトピックは無く、将来用の先行整備）。
- 情報系＝スレート左バー／実践系＝琥珀左バー、という左バーの色でカテゴリを示す規約に統一。CSS変数のみ・トークン参照。HTML/JS/グラフ/数式には未介入。

**影響範囲**: トピックページの注記(`.note`)とまとめ(`.keypoints`)。実効的に画面へ出るのはこの2種（例題は先行整備）。

**before-afterで良くなった点**（1360px 実機スクショで確認）
- 注記とまとめが同じ「角丸＋スレート左バー」で揃い、ページ内のコールアウトが一貫した family として読めるように。以前はまとめだけ左バーが無く、注記と別物に見えていた。
- 左バーの色（スレート＝情報／琥珀＝実践）でカテゴリを示す規約ができ、彩度を増やさずに区別を表現。「学術的ミニマル」を維持。
- KaTeX・グラフ・コントラスト・モバイルは不変（回帰なし）。

**次の改善候補**
1. **サイドバーの視線誘導**: グループ見出しと項目のインデント・現在地表示の強調度、`details` 開閉マーカーの調整。
2. **タイポグラフィ階層の微調整**: `.lead` 下の 2px 実線（墨）の重さ、h3 直後の余白、リスト行間の再点検。
3. **ページャ／パンくずの一貫性**: 前後リンクとパンくずの余白・ホバーの統一。

---

## 2026-07-06 — Iter.D4: 本文リストのタイポグラフィ・リズム

**改善テーマ**: 本文の箇条書き（ul/ol）を段落の縦リズムに揃える。
**変更内容**: `.body ul,.body ol { margin:0.75em 0; padding-left:1.5em; }` で既定40pxインデントを詰め段落と左端を揃え、`.body li { margin:0.3em 0; }` で項目間に余白、`.body li::marker { color:var(--muted-2); }` でマーカーを無彩色に。CSSのみ・トークン参照。
**影響範囲**: トピック本文の箇条書きのみ。表・デモ・数式・コールアウトは不変。
**before-after**: 番号リストが段落と左端で揃い、多行項目の切れ目が明確に。マーカーが控えめで落ち着いた階層に（GLMページ実機確認）。
**次の改善候補**: サイドバー視線誘導／ページャ一貫性／`.lead` 下線の重さ。

---

## 2026-07-06 — Iter.D5: サイドバー現在地表示の軽量化

**改善テーマ**: 現在地（active）の表示を「濃いスレート全面塗り」から、D3のコールアウト言語に揃えた「淡い面＋スレート左バー」に。
**変更内容**: `#sidebar a.active` を `background:var(--surface); color:var(--ink); font-weight:700; box-shadow: inset 3px 0 0 var(--accent);` に変更。重い暗色ベタ塗りをやめ、左3pxのスレートバーで現在地を示す（レイアウトシフトなしの inset box-shadow）。
**影響範囲**: サイドバーの選択項目の見た目のみ。ナビ構造・リンク挙動は不変。
**before-after**: 現在地表示が軽くなり、コールアウト（注記・まとめ）の左バーと同じ視覚言語で統一。情報密度の高いナビでも「主張しすぎない現在地」に。コントラストは ink on surface で AA 確保。
**次の改善候補**: `details` 開閉マーカーの微調整／グループ見出しの階層強調。

---

## 2026-07-06 — Iter.D6: ページャのホバーをアクセントに統一

**改善テーマ**: 前後リンク（ページャ）のホバー色を、サイト全体のスレートアクセント体系に合わせる。
**変更内容**: `.pager .pg:hover` の `border-color: var(--ink)` → `var(--accent)`。カードやサイドバー等の「ホバーでスレート」に揃え、墨（黒）ホバーの浮きをなくした。
**影響範囲**: トピック下部の前後リンクのホバー時のみ。
**before-after**: 前後リンクのホバーが他のインタラクティブ要素と同じスレートになり、ホバーの色規約が一貫。
**次の改善候補**: パンくず（crumbs）のホバー余白の微調整。

---

## 2026-07-06 — Iter.D7: リード文下線の軽量化

**改善テーマ**: 要約（`.lead`）下の 2px 墨の実線が「学術的ミニマル」にはやや重かったので軽くする。
**変更内容**: `.lead` の `border-bottom: 2px solid var(--ink)` → `1px solid var(--line-strong)`、`padding-bottom: 18px` → `16px`。
**影響範囲**: トピックページの要約文と本文の境界線のみ。
**before-after**: 要約と本文の区切りが太い黒線から細い罫線になり、圧迫感が減って落ち着いた印象に（MLEページ実機で 1px 罫線を確認）。情報の区切りは保ちつつ装飾の重さを削減。
**次の改善候補**: h3 直後の余白リズム／カードのホバー影の強さの再点検。
